#!/usr/bin/env python3
"""Generate Rust dataset from Python colour-science MUNSELL_COLOURS_ALL."""

import sys
sys.path.insert(0, '/Users/chris/Dropbox/dev/projects/libraries/MunsellSpace/venv_comparison/lib/python3.13/site-packages')

from colour.notation.datasets.munsell.all import MUNSELL_COLOURS_ALL

print("""// AUTO-GENERATED from Python colour-science MUNSELL_COLOURS_ALL
// DO NOT EDIT MANUALLY
// Generated by generate_rust_dataset.py

use crate::MunsellError;
use crate::Result;

/// Complete Munsell Renotation Dataset
/// From colour-science: https://github.com/colour-science/colour
/// Total entries: 4995
/// Format: ((hue_str, value, chroma), (x, y, Y))
/// 
/// IMPORTANT: Y values in this dataset are scaled by 1/0.975 factor
/// as per the original Munsell renotation data files.
/// This matches the Python colour-science library exactly.
pub const MUNSELL_RENOTATION_DATA: &[((&str, f64, f64), (f64, f64, f64))] = &[""")

for (hue, value, chroma), (x, y, Y) in MUNSELL_COLOURS_ALL:
    # Use exact values from Python, no scaling
    print(f'    (("{hue}", {value}, {chroma}), ({x}, {y}, {Y})),')

print("];")
print()
print(f"// Total entries: {len(MUNSELL_COLOURS_ALL)}")